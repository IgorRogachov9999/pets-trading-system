# Log Entry - 2026-03-20 18:40:34 UTC

## User Request

find why atlasian mcp fails and how to fix it

## Response Summary

Diagnosed the Atlassian MCP failure by checking Docker daemon status, inspecting .mcp.json, testing the mcp/atlassian container directly with an MCP initialize handshake, and verifying stdout/stderr separation. Found two root causes: (1) the old API token caused Confluence 403 errors (already fixed by updating .mcp.json in the previous step), and (2) the MCP is not active in the current session because the /mcp dialog was dismissed. Confirmed the Docker container and protocol are healthy — banner output goes to stderr only, stdout is clean JSON-RPC. Recommended running /mcp and clicking Connect on atlassian, and optionally pulling the latest mcp/atlassian image (2.14.5 installed, 3.1.1 available).
