# Log Entry - 2026-03-21 17:48:44 UTC

## User Request

changed back to oauth [same "Invalid username or token" errors from Sourcetree]

## Response Summary

Noted that the branch is already pushed to GitHub and the Sourcetree issue only blocks future pushes. Explained that the OAuth token appears expired due to multiple account-switching attempts. Recommended revoking Sourcetree's OAuth authorization on GitHub (Settings → Applications → Authorized OAuth Apps) and re-adding the GitHub account in Sourcetree to get a fresh token. Clarified that even after fix, Sourcetree OAuth cannot push `.github/workflows/` files (requires `workflow` scope unavailable to OAuth apps), so terminal pushes via `! git push origin <branch>` should be used for those.
