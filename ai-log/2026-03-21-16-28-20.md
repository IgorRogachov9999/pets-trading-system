# Log Entry - 2026-03-21 16:28:20 UTC

## User Request

do not create new tickets for the same task when previous are not done, just move them to in progress, save it to momory to remember. Remove that extra stories, and return tasks from done to in progress, and the main story also (You alrady did it before contect compacting so ignore it). So, about terraform changes - 1 There should be one ecr for all envs, thing before doing such things, so on ecr should not be env and tags for env, this is a shared resource. 2 remove iam module, distribute iam policies to related modules in separate files iam.tf inside each module. 3 Also fix namings for env - when you have separation with / use env first like /env/service-name to keep it usable. 4 add default tags - environment=env and terraform=true to mark all the terraform resources, and share that default tags to all resources. 5 move hardcoded important configurations to env configs, such as cpu and memory for ecs tasks. 6 networking main file is too big, split it to separate vpc, subnets and other stuf. 7 in general make it moreconfigurable. 8 rename prod to demo, there should be only dev and demo envs use official terraform skills to make the modules

## Response Summary

Resumed session after context compaction. Fetched PTS-17 through PTS-21 from Jira to confirm all are In Progress. Updated PTS-20's description to reflect the full 8-item Terraform rework scope. Dispatched the `senior-devops-engineer` background agent with a comprehensive prompt covering: (1) ECR repo names without env suffix, (2) IAM module dissolution into `ecs/iam.tf`, `lambda/iam.tf`, and root `terraform/iam.tf`, (3) log group naming to `/${env}/service-name`, (4) default_tags with `Environment` + `Terraform = "true"`, (5) `environments/dev.tfvars` and `environments/demo.tfvars` with ECS cpu/memory and log retention, (6) splitting `networking/main.tf` into 5 focused files, (7) general configurability for log retention and RDS backup, (8) `prod` → `demo` rename throughout. Agent will transition PTS-20 and PTS-21 to In Review when complete.
