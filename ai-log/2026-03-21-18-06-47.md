# Log Entry - 2026-03-21 18:06:47 UTC

## User Request

update @.gitignore to ignore local files for terraform and react. Also when you do push load all not pushed files from @ai-log/ and save this to meory to remeber about it

## Response Summary

Updated root `.gitignore` to add Terraform local patterns (`.terraform/`, lock files, state/plan files, crash logs, override files) and React/Node patterns (`node_modules/`, `dist/`, `.env*`, `coverage/` scoped to `src/ui/`). Saved a feedback memory at `memory/feedback_push_ai_logs.md` (with MEMORY.md index updated) instructing that all untracked `ai-log/` files must be staged and committed before every push. Committed `.gitignore` changes together with three previously untracked `ai-log/` entries and pushed to `feature/Initial-setup`.
