# Log Entry - 2026-03-21 16:38:59 UTC

## User Request

You are working on Jira ticket **PTS-20: Terraform Infrastructure Rework**.

This ticket is already In Progress. When implementation is complete, transition ticket PTS-20 to "In Review" using transition_id 41, and also transition PTS-21 to "In Review" if you update any CI/CD workflow files.

[... full task specification with 8 required change sets across ECR, IAM, log groups, default tags, tfvars, networking split, general configurability, and prod→demo rename ...]

## Response Summary

Implemented all 8 Terraform rework change sets for PTS-20. Key changes: (1) removed environment suffix from ECR repo names in `terraform/modules/ecr/main.tf` (ECR is shared); (2) deleted `terraform/modules/iam/` entirely and distributed IAM roles to `terraform/modules/ecs/iam.tf`, `terraform/modules/lambda/iam.tf`, and the new root-level `terraform/iam.tf` for GitHub OIDC; (3) updated CloudWatch log group names to `/${environment}/trading-api` and `/${environment}/lifecycle-engine` in `ecs/main.tf` and `lambda/main.tf`; (4) added `Terraform = "true"` to provider `default_tags` in `terraform/main.tf`; (5) externalized ECS cpu/memory and log_retention_days to variables, created `terraform/environments/dev.tfvars` and `terraform/environments/demo.tfvars`, deleted `prod.tfvars`; (6) split `terraform/modules/networking/main.tf` (446 lines) into five focused files: `vpc.tf`, `subnets.tf`, `routing.tf`, `security_groups.tf`, `endpoints.tf`; (7) replaced hardcoded `retention_in_days` and `us-east-1` region with variables; (8) renamed all `"prod"` environment references to `"demo"` across all `.tf` files and descriptions. Updated `terraform/main.tf`, `terraform/variables.tf`, `terraform/outputs.tf`, module variables and outputs. For CI/CD (PTS-21): deleted `deploy-prod.yml`, created `deploy-demo.yml`, updated `deploy-dev.yml` and `post-merge-build.yml` ECR repo name references. Updated `docs/stories/TS-001/task-3-terraform.md` with full change summary. Transitioned both PTS-20 and PTS-21 to "In Review".
