# Log Entry - 2026-03-22 00:27:53 UTC

## User Request

separate backend pr check to api an lambda as separate pipelines checks

## Response Summary

Created two new PR check pipelines and superseded the old combined one. Created `.github/workflows/trading-api-pr-check.yml` — triggers on changes to `src/trading-api/src/PetsTrading.TradingApi/**`, shared projects (Application, Domain, Infrastructure), and `tests/PetsTrading.TradingApi.Tests/**`; restores the full solution then builds `PetsTrading.TradingApi.csproj` and tests `PetsTrading.TradingApi.Tests.csproj`. Created `.github/workflows/lifecycle-lambda-pr-check.yml` — triggers on changes to `src/trading-api/src/lifecycle/**`, the same shared projects, and `tests/PetsTrading.LifecycleLambda.Tests/**`; builds `PetsTrading.LifecycleLambda.csproj` and tests `PetsTrading.LifecycleLambda.Tests.csproj`. Updated `.github/workflows/backend-pr-check.yml` to a superseded stub (workflow_dispatch only, exits 1 with a notice pointing to the two new files).
