# Log Entry - 2026-03-20 20:50:54 UTC

## User Request

add mcp for aws documentation and github documentation

## Response Summary

Added two MCP server entries to `ai-env.json`: `aws-documentation` (npx transport, package `@awslabs/aws-documentation-mcp-server`) and `github` (docker transport, image `ghcr.io/github/github-mcp-server`, requires `GITHUB_PERSONAL_ACCESS_TOKEN`). Applied the `aws-documentation` server to `.mcp.json` immediately since it requires no secrets. The `github` server was blocked from being written to `.mcp.json` because `GITHUB_PERSONAL_ACCESS_TOKEN` is absent from `.env`; user was instructed to add it and re-run `ai-env`.
