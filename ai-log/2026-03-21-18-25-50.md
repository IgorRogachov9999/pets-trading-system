# Log Entry - 2026-03-21 18:25:50 UTC

## User Request

wait, so now it will be deploying all the dev and demo build to the same claudefront?

## Response Summary

Clarified that dev and demo each have their own separate CloudFront distribution and S3 frontend bucket (`{PROJECT_NAME}-dev-frontend` vs `{PROJECT_NAME}-demo-frontend`). Explained that the auto-discovery query filters by the bucket name in the CloudFront origin domain, so each workflow's invalidation step targets only the distribution for its own environment — they do not share a distribution.
