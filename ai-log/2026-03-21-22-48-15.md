# Log Entry - 2026-03-21 22:48:15 UTC

## User Request

also revert changes from this commit [screenshot of commit 9637db2]

## Response Summary

Reverted the changes introduced in commit `9637db2` ("Fix ECS deploy to use latest task definition after Terraform apply") across three GitHub Actions workflow files. Removed `TASK_FAMILY` and `LATEST_TASK_DEF` variable resolution (via `aws ecs describe-task-definition`) and the `--task-definition` flag from `aws ecs update-service` calls in `.github/workflows/deploy-dev.yml`, `.github/workflows/deploy-demo.yml`, and `.github/workflows/initial-setup.yml`. The original comment in `deploy-demo.yml` was restored. No other files were touched.
