# Log Entry - 2026-03-22 00:31:16 UTC

## User Request

now separate @.github/workflows/post-merge-build.yml to separate pipelines for ui, api and lambda. Each pipeline should be triggered only if there were changes in its files

## Response Summary

Created three new push-triggered build pipelines replacing `post-merge-build.yml`. Created `.github/workflows/build-trading-api.yml` — triggers on push to main when `src/trading-api/src/PetsTrading.TradingApi/**` or any shared project (Application, Domain, Infrastructure) changes; builds and pushes the Trading API Docker image to ECR with `github.sha` and `latest` tags. Created `.github/workflows/build-lifecycle-lambda.yml` — triggers on `src/trading-api/src/lifecycle/**` and shared project changes; builds and pushes the Lifecycle Lambda Docker image. Created `.github/workflows/build-frontend.yml` — triggers on `src/ui/**` changes; resolves the API Gateway URL, runs `npm ci`, `npm test`, `npm run build`, then uploads the dist to S3 builds bucket under `frontend/{sha}/` and `frontend/latest/`. Updated `.github/workflows/post-merge-build.yml` to a superseded stub pointing to the three new files. All three new pipelines also support `workflow_dispatch` for manual triggering.
