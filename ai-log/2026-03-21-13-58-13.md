# Log Entry - 2026-03-21 13:58:13 UTC

## User Request

ai-env:ai-env run the mcp for terraform docs in docker

## Response Summary

Pulled `mcp/aws-terraform:latest` Docker image (digest sha256:b078ccd6...) and verified it responds correctly to an MCP `initialize` probe — server identified as `terraform_mcp_server v1.25.0`. Updated `ai-env.lock.json` to record the image digest and server version for the `aws-terraform` entry. Noted to the user that the `mcp/aws-terraform` image is marked deprecated by its maintainer, with a recommendation to migrate to HashiCorp's official Terraform MCP Server.
