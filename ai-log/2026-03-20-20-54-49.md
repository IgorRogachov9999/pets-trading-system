# Log Entry - 2026-03-20 20:54:49 UTC

## User Request

fix the aws documentation mcp, it does not work, use docker version https://hub.docker.com/r/mcp/aws-documentation

## Response Summary

Updated the `aws-documentation` MCP server configuration from npx to Docker transport. In `ai-env.json`, changed the entry's `transport` from `"npx"` and `package` field to `"docker"` with `image: "mcp/aws-documentation"`. In `.mcp.json`, replaced the npx-based command with `docker run --rm -i mcp/aws-documentation`. User was advised to restart Claude Code to reconnect the server.
