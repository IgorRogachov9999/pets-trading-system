# Log Entry - 2026-03-21 13:15:47 UTC

## User Request

update backend-skill to add refferences for unit testing, it should write tests like here https://github.com/IgorRogachov9999/TaskManager/tree/main/tests/TaskManager.Domain.Tests, the same style, libs and aaa workflow. Also add some reffernces to use automapper for mappings. Also add refferences about dapper, find docs and usecases about it usage and best practices

## Response Summary

Added three new reference files to `.claude/skills/backend-skill/references/`: `unit-testing.md` (xUnit + FluentAssertions + NSubstitute, AAA pattern, naming convention `MethodName_Condition_ExpectedOutcome`, domain/service/integration test examples with Testcontainers, Test Data Builders for `TraderBuilder`/`ListingBuilder`, coverage targets and CI config), `automapper.md` (Profile-based DI setup, `TradingProfile` mappings for all domain DTOs, `IValueResolver` pattern, in-memory mapping with Dapper, `AssertConfigurationIsValid()` test pattern, common pitfalls table), and `dapper-advanced.md` (full Dapper API reference sourced from learndapper.com, `NpgsqlDataSource` pooling, transaction pattern for atomic bid replacement with `FOR UPDATE`, multi-mapping with `splitOn`, `QueryMultipleAsync` for dashboard queries, buffered vs unbuffered, repository pattern, `DefaultTypeMap.MatchNamesWithUnderscores`, pitfalls table). Note: the referenced TaskManager GitHub repo (`IgorRogachov9999/TaskManager`) returned 404 and could not be accessed; standard .NET testing conventions were used instead. `SKILL.md` sections 3 and 9 were updated to reference the new files and highlight key rules (Testcontainers for integration tests, `AssertConfigurationIsValid` per profile, `DefaultTypeMap.MatchNamesWithUnderscores`).
