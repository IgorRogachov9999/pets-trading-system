# Log Entry - 2026-03-22 01:43:52 UTC

## User Request

I'm the team lead planning authentication frontend tasks for the pets-trading-system. I need your technical input BEFORE creating Jira tickets. Do NOT write any code yet — just provide a detailed technical plan.

**Architecture context:**
- Authentication: Amazon Cognito User Pools (ADR-006)
- Frontend: React 18, TypeScript, Vite, TanStack Query, React Router v6, Tailwind CSS
- Auth: Cognito JWT stored in browser, attached as `Authorization: Bearer` header on all API calls
- No auth library installed yet (no `@aws-amplify/auth` or `amazon-cognito-identity-js` in package.json)
- File structure: `src/features/auth/`, `src/api/`, `src/hooks/`, `src/router/`, `src/components/`

**Current frontend state:**
- `LoginPage.tsx` and `RegisterPage.tsx`: stubs returning `<h1>` only
- `router/index.tsx`: routes exist for /login, /register, and protected routes but NO auth guard
- `api/client.ts`: `apiFetch` wrapper with optional Bearer token support — but nothing passes a token yet
- No AuthContext, no auth state management

**Authentication requirements:**
1. US-000.1 (PTS-29): Register form (email + password) → Cognito → create trader in backend → auto-login → redirect to /market
2. US-000.2 (PTS-30): Login form (email + password) → Cognito → load trader state → redirect to /market
3. US-000.3 (PTS-31): Logout button → Cognito GlobalSignOut → clear local state → redirect to /login; back button should not expose trader panel
4. US-000.4 (PTS-32): All routes under AppShell require auth — unauthenticated requests redirect to /login; expired session redirects to /login with message

**Backend API contracts (for your planning):**
- After Cognito registration succeeds, frontend calls `POST /api/v1/auth/register` with the JWT to create the trader record ($150 cash)
- `GET /api/v1/traders/me` returns the current trader's state

**Questions I need answered:**

1. Which Cognito SDK should we use?
2. What's the auth state management approach?
3. What exactly is the AuthContext shape?
4. How does the protected route work in React Router v6?
5. How does the token get attached to all API calls?
6. What new files need to be created vs what gets modified?
7. What Vitest tests are needed for the auth feature?

## Response Summary

Read the current state of `src/ui/src/router/index.tsx`, `src/ui/src/api/client.ts`, `src/ui/src/features/auth/LoginPage.tsx`, `src/ui/src/features/auth/RegisterPage.tsx`, `src/ui/src/components/layout/AppShell.tsx`, `src/ui/src/components/layout/Header.tsx`, `src/ui/src/types/trader.ts`, `src/ui/src/hooks/useWebSocket.ts`, `src/ui/src/vite-env.d.ts`, and `src/ui/package.json` to ground the plan in actual code. Produced a full technical specification covering all 7 questions: recommended `amazon-cognito-identity-js` over Amplify v6 (bundle size and in-memory token control), specified React Context + useReducer for auth state with an `AuthState` discriminated union, defined the full `AuthContextValue` interface including `AuthUser` shape and `scheduleRefresh` logic, specified a `ProtectedRoute` component wrapping the AppShell subtree in `router/index.tsx` with post-login redirect-back via `location.state.from`, specified that tokens flow from `useAuth()` into each TanStack Query hook's `queryFn` (not read inside `apiFetch` itself), listed 8 new files to create and 11 files to modify with their exact purposes, and defined 5 test files covering `AuthContext`, `ProtectedRoute`, `LoginPage`, `RegisterPage`, and `cognito.ts` with specific test cases for each.
