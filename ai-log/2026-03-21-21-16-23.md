# Log Entry - 2026-03-21 21:16:23 UTC

## User Request

fix pipelines https://github.com/IgorRogachov9999/pets-trading-system/actions/runs/23388661861/job/68040202228?pr=3, Run docker build \
  docker build \
    -t :4398cc450800fb636cc6267ef7d43547a98cccd7 \
    -t :latest \
    -f src/trading-api/src/PetsTrading.TradingApi/Dockerfile \
    src/trading-api/
  docker push :4398cc450800fb636cc6267ef7d43547a98cccd7
  docker push :latest
  shell: /usr/bin/bash -e {0}
  env:
    AWS_REGION: ***
    PROJECT_NAME: ***
    ENVIRONMENT: dev
    ECR_TRADING_API:
    ECR_LIFECYCLE:
...
ERROR: failed to build: invalid tag ":4398cc450800fb636cc6267ef7d43547a98cccd7": invalid reference format

## Response Summary

Diagnosed the root cause as GitHub Actions silently suppressing job outputs that contain masked secret values — since `PROJECT_NAME` is a masked GHA secret, any Terraform output containing it (`ECR_TRADING_API`, `ECR_LIFECYCLE`, `builds_bucket`, `frontend_bucket`, `ecs_cluster`, `ecs_service`, `lambda_function`) was dropped as an empty string. Fixed `.github/workflows/initial-setup.yml` by eliminating all inter-job output passing for those values and replacing them with inline computations using deterministic naming patterns: ECR URLs computed via `aws sts get-caller-identity`, bucket names from `${{ env.PROJECT_NAME }}-builds` / `${{ env.PROJECT_NAME }}-frontend-${{ env.ENVIRONMENT }}`, ECS cluster/service from `${{ env.PROJECT_NAME }}-${{ env.ENVIRONMENT }}-cluster/trading-api`, and Lambda from `${{ env.PROJECT_NAME }}-${{ env.ENVIRONMENT }}-lifecycle-lambda`. The `cloudfront_id` output (a random AWS ID containing no project name) continues to pass through job outputs. Committed as `a19a87c` and pushed to `fix/Fix-pipeline-2`.
