# Log Entry - 2026-03-21 15:25:22 UTC

## User Request

You are working on Jira ticket PTS-21: "CI/CD — GitHub Actions PR checks and deploy pipelines"

Transition PTS-21 to In Progress when you start, and Done when complete.

## Context

Part of technical story PTS-17 (Project Base Setup). Create 4 GitHub Actions workflow files under `.github/workflows/` in the repository at `/Users/ihorrohachov/github/pets-trading-system/`.

The dependent tasks are complete:
- **Backend** (PTS-18): `src/trading-api/TradingApi.sln` — .NET 10, builds with `dotnet build`, tests with `dotnet test`
- **Frontend** (PTS-19): `src/ui/package.json` — node 20, scripts: `build` (vite), `test` (vitest run)
- **Terraform** (PTS-20): `terraform/` — outputs: `ecr_repository_url`, `cloudfront_domain_name`, `frontend_s3_bucket_name`

## What to Build

Create `.github/workflows/` directory and the following 4 files: [backend-pr-check.yml, frontend-pr-check.yml, backend-deploy.yml, frontend-deploy.yml]

## Response Summary

Transitioned PTS-21 from its initial state to In Progress, then to Done via Jira transitions (IDs 31 and 51). Created the `.github/workflows/` directory and wrote 4 GitHub Actions workflow files: `/Users/ihorrohachov/github/pets-trading-system/.github/workflows/backend-pr-check.yml` (pull_request trigger on `src/trading-api/**`, dotnet 10 restore/build/test with TRX artifact upload), `/Users/ihorrohachov/github/pets-trading-system/.github/workflows/frontend-pr-check.yml` (pull_request trigger on `src/ui/**`, Node 20 npm ci/build/test with cache), `/Users/ihorrohachov/github/pets-trading-system/.github/workflows/backend-deploy.yml` (push to main on `src/trading-api/**`, OIDC auth via `secrets.AWS_ROLE_ARN`, ECR login, docker build+push tagged with `github.sha` and `latest`), and `/Users/ihorrohachov/github/pets-trading-system/.github/workflows/frontend-deploy.yml` (push to main on `src/ui/**`, Node 20 build, OIDC auth, S3 sync with --delete, CloudFront invalidation). Both deploy workflows have `id-token: write` and `contents: read` permissions at the workflow level. Verified that `docs/stories/TS-001/task-4-cicd.md` already contained an accurate secrets table matching the ticket requirements — no changes needed.
