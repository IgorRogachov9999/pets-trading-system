# Log Entry - 2026-03-20 22:59:58 UTC

## User Request

@"solution-architect (agent)" @docs/architecture/05-building-block-view.md i see that lifecycle engine is a ecs task. It seems like it easier to use lambda that is cheper and can be scheduled by event bridge schedules to run every minute. Also runnning it every minute may lead a bug because customer can buy a new pet from first market at 59 seconds and the lifetime will decrieased anyway. Find better solution to manage the lifetime and sugest them before writing docs

## Response Summary

The solution-architect agent produced an analysis-only response (no files changed). It recommended moving the Lifecycle Engine from an ECS Fargate singleton BackgroundService to a Lambda + EventBridge Scheduler (rate: 1 minute), citing ~$0 cost vs $10-15/month for ECS, elimination of singleton coordination complexity, and alignment with ADR-008. For the timing bug, it recommended Option F (absolute timestamp-based aging): age is always derived from `created_at` rather than incremented per tick, while health/desirability variance remains tick-based; `pets.age` is kept as a cache column refreshed each tick. Two new ADRs (ADR-015 and ADR-016) were proposed. The agent asked for user confirmation on 5 points before making any file or Confluence changes.
