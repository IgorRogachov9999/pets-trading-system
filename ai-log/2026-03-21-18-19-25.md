# Log Entry - 2026-03-21 18:19:25 UTC

## User Request

Fix `.github/workflows/deploy-demo.yml` to auto-discover the CloudFront distribution ID at runtime instead of requiring it as a GitHub secret.

**Current code** (around line 133-138):
```yaml
- name: Invalidate CloudFront
  # DEMO_CLOUDFRONT_DISTRIBUTION_ID is set after first demo terraform apply
  run: |
    aws cloudfront create-invalidation \
      --distribution-id ${{ secrets.DEMO_CLOUDFRONT_DISTRIBUTION_ID }} \
      --paths "/*"
```

**Replace with** the same auto-discovery pattern used in `deploy-dev.yml` (lines 131-144), adapted for the demo environment:
```yaml
- name: Invalidate CloudFront
  run: |
    FRONTEND_BUCKET="${{ secrets.PROJECT_NAME }}-demo-frontend"
    DIST_ID=$(aws cloudfront list-distributions \
      --query "DistributionList.Items[?Origins.Items[0].DomainName==\`${FRONTEND_BUCKET}.s3.amazonaws.com\`].Id" \
      --output text)
    if [ -n "$DIST_ID" ] && [ "$DIST_ID" != "None" ]; then
      aws cloudfront create-invalidation \
        --distribution-id ${DIST_ID} \
        --paths "/*"
    else
      echo "WARNING: CloudFront distribution not found for ${FRONTEND_BUCKET} — skipping invalidation"
    fi
```

Also update the comment block at the top of the file (lines 1-13) to remove `DEMO_CLOUDFRONT_DISTRIBUTION_ID` from the "Required GitHub Secrets" list.

Do not create any Jira tickets. Just make the file change and confirm what was done.

## Response Summary

Two edits were made to `/Users/ihorrohachov/github/pets-trading-system/.github/workflows/deploy-demo.yml`. First, the `DEMO_CLOUDFRONT_DISTRIBUTION_ID` entry was removed from the "Required GitHub Secrets" comment block at the top of the file (line 12). Second, the "Invalidate CloudFront" step (lines 133-138) was replaced with the auto-discovery pattern from `deploy-dev.yml`, which queries `aws cloudfront list-distributions` using a JMESPath filter on the S3 origin domain name (`${PROJECT_NAME}-demo-frontend.s3.amazonaws.com`) and includes a non-fatal fallback warning if no distribution is found. The `DEMO_CLOUDFRONT_DISTRIBUTION_ID` GitHub secret is no longer referenced anywhere in the workflow.
