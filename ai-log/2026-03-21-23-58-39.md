# Log Entry - 2026-03-21 23:58:39 UTC

## User Request

ok, ecs works now, health endpoint is also working. Now we need to fix frontend, i get this responses when calling the api from ui, the same result from direct api call. Find how to fix it

## Response Summary

Diagnosed two root causes from the browser network screenshot: (1) 401 on OPTIONS preflight — API Gateway's catch-all {proxy+} has COGNITO_USER_POOLS auth which blocks unauthenticated CORS preflight requests; (2) /test endpoint was not a dedicated API Gateway resource so it was routed through the Cognito-gated catch-all. Dispatched senior-devops-engineer to add a /test resource with NONE auth and OPTIONS MOCK integrations on both {proxy+} and /test in terraform/modules/api-gateway/main.tf, and updated the redeployment trigger hash with all 20 entries. Dispatched senior-dotnet-dev to replace the existing AllowAnyOrigin CORS policy in Program.cs with a named "FrontendPolicy" allowing the CloudFront origin and localhost dev ports, placed before auth middleware.
