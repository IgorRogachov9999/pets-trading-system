# Log Entry - 2026-03-21 18:45:12 UTC

## User Request

no, create separate terraform for ecr with its own state, it should be shared betwean environments but if it is in the main terraform next environment will try to override it and fail. Also update pipelines to run ecr terraform first which is single state for all envs and then for env with its own state. Also check the deployment action and fix it

## Response Summary

Dispatched senior-devops-engineer to extract ECR into a standalone Terraform root configuration. Created `terraform/ecr/main.tf`, `terraform/ecr/variables.tf`, `terraform/ecr/outputs.tf`, and `terraform/ecr/versions.tf` with its own shared state key `pts/ecr/terraform.tfstate`. Removed `module "ecr"` from `terraform/main.tf` and replaced with `data "aws_ecr_repository"` data sources; updated all four ECR outputs in `terraform/outputs.tf` accordingly. Updated `initial-setup.yml`, `deploy-dev.yml`, and `deploy-demo.yml` to add a `terraform-ecr` job that runs before env-specific Terraform, with correct job dependency chains. Noted that existing dev Terraform state requires `terraform state rm module.ecr.*` before next pipeline run to avoid state ownership conflicts.
