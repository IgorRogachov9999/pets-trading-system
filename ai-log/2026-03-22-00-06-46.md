# Log Entry - 2026-03-22 00:06:46 UTC

## User Request

Fix the frontend API path for the test endpoint.

## Problem

In `src/ui/src/api/test.ts`, the `fetchTest()` function calls `/api/v1/test`. This path hits the API Gateway catch-all `{proxy+}` route which requires Cognito auth — causing 401 + CORS errors.

## Fix

API Gateway has a dedicated `/test` resource (no auth) that proxies to the backend's `/api/v1/test`. So the frontend should call `/test`, not `/api/v1/test`.

1. Read `src/ui/src/api/test.ts` — change the path from `/api/v1/test` to `/test`
2. Read `src/ui/src/features/market/MarketPage.test.tsx` — update any mock URL from `/api/v1/test` to `/test` if present
3. Run tests: `cd src/ui && npm test -- --run`

Only modify these files. Do not create new files.

## Response Summary

Read `src/ui/src/api/test.ts` and `src/ui/src/features/market/MarketPage.test.tsx`. Both files were already correct: `test.ts` already calls `/test` (not `/api/v1/test`), and the test file uses `globalThis.fetch` mocks without URL-specific matching so no update was needed. Ran `npm test -- --run` in `src/ui/` and all 8 tests passed. No files were modified.
