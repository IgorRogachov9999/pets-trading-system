# Log Entry - 2026-03-21 19:28:56 UTC

## User Request

added the secret, update the pipelines to remove its creation and just load it from github secrets

## Response Summary

Simplified `terraform/iam.tf` from 197 lines (OIDC provider resource, IAM role resource, IAM role policy) down to 13 lines (data sources only + explanatory comment), removing all Terraform-managed IAM resources that are now manually managed. Removed `github_actions_role_arn` output from `terraform/outputs.tf` and the `github_repo` variable from `terraform/variables.tf`, `terraform/environments/dev.tfvars`, and `terraform/environments/demo.tfvars`. Confirmed pipelines already use `${{ secrets.AWS_ROLE_ARN }}` throughout — no pipeline changes needed. Committed as `226053b` and pushed to `fix/Fix-pipeline-2`.
