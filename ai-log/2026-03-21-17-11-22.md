# Log Entry - 2026-03-21 17:11:22 UTC

## User Request

ok, now name all settings that i need to add to github env to make the pipelines work

## Response Summary

Read all 8 GitHub Actions workflow files under .github/workflows/ and extracted all secrets/variables referenced. Identified 6 required GitHub Secrets: AWS_ROLE_ARN (GitHub OIDC IAM role ARN), AWS_REGION (us-east-1), PROJECT_NAME (pts), TF_STATE_BUCKET (S3 bucket for Terraform state), TF_STATE_LOCK_TABLE (DynamoDB table for state locking), and DEMO_CLOUDFRONT_DISTRIBUTION_ID (set after first demo Terraform apply). Also documented the one-time manual AWS prerequisites (create S3 state bucket and DynamoDB lock table before any pipeline runs) and the correct bootstrap order for first-time setup.
