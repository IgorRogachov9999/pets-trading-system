# Log Entry - 2026-03-21 18:24:29 UTC

## User Request

explain more why do we need that claudefront id and how it is used

## Response Summary

Explained CloudFront cache invalidation: why it is needed (CDN caches S3 files and doesn't auto-refresh on new deploys), how the `create-invalidation` API call uses the distribution ID to target the correct CloudFront distribution, and the two approaches to obtaining the ID at runtime (auto-discovery via `list-distributions` query vs. storing as a secret). Also clarified why `index.html` is the critical file to invalidate (uploaded with `no-cache`) while hashed JS/CSS assets self-invalidate via filename changes on each build.
