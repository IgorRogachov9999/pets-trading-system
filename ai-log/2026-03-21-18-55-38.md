# Log Entry - 2026-03-21 18:55:38 UTC

## User Request

You are implementing a shared frontend builds S3 bucket for the pets-trading-system project. Read every file listed before making any changes.

## Context

The frontend builds bucket currently lives inside the per-environment `terraform/modules/s3-cloudfront/` module, named `${var.project_name}-${var.environment}-builds` (e.g., `pts-dev-builds`, `pts-demo-builds`). This means dev and demo each have separate builds buckets. The problem: `initial-setup.yml` only populates `pts-dev-builds`, so there is nothing in `pts-demo-builds` when you try to deploy to demo.

The solution: one shared builds bucket `${var.project_name}-builds` (no env suffix), managed in its own Terraform state `pts/builds/terraform.tfstate`, similar to how ECR was just extracted into `terraform/ecr/`. All environments build once and deploy any SHA from the shared bucket.

## Response Summary

Created the standalone `terraform/builds/` root configuration with four files (`main.tf`, `variables.tf`, `outputs.tf`, `versions.tf`) following the exact pattern of `terraform/ecr/`. The builds bucket is named `${var.project_name}-builds` (no environment suffix) with versioning, public access block, AES256 SSE, and a 90-day lifecycle expiry rule. Removed the four builds bucket resources (`aws_s3_bucket.builds`, `aws_s3_bucket_versioning.builds`, `aws_s3_bucket_public_access_block.builds`, `aws_s3_bucket_server_side_encryption_configuration.builds`) and the `builds_bucket_name` output from `terraform/modules/s3-cloudfront/`. Added `data "aws_s3_bucket" "builds"` to `terraform/main.tf` after the ECR data sources and updated `terraform/outputs.tf` to reference `data.aws_s3_bucket.builds.bucket`. Updated all four workflow files: `initial-setup.yml` gained a parallel `terraform-builds` job with outputs, `terraform-apply` now needs both shared jobs, and `build-frontend`/`deploy` reference `needs.terraform-builds.outputs.builds_bucket`; `deploy-dev.yml` and `deploy-demo.yml` each gained a parallel `terraform-builds` job and the hardcoded `BUILDS_BUCKET` value was changed from `$PROJECT_NAME-{env}-builds` to `$PROJECT_NAME-builds`; `post-merge-build.yml` had the `environment` workflow_dispatch input removed (it was only used for the bucket name) and the bucket name updated. All `.tf` files passed `terraform fmt` with no changes required.
