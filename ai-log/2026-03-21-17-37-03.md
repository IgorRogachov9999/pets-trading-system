# Log Entry - 2026-03-21 17:37:03 UTC

## User Request

changed to personal token with full access but is still does not work [authentication errors from Sourcetree]

## Response Summary

Diagnosed that macOS Keychain has cached old GitHub OAuth credentials that override Sourcetree's newly configured PAT. Provided two remediation options: (1) run `git credential-osxkeychain erase` from the terminal to clear cached github.com credentials, (2) manually delete github.com entries from the Keychain Access app. Instructed the user to retry the push in Sourcetree after clearing the cache, at which point it will prompt for credentials and use the new PAT.
