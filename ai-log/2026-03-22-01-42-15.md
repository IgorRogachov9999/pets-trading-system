# Log Entry - 2026-03-22 01:42:15 UTC

## User Request

I'm the team lead planning authentication infrastructure tasks for the pets-trading-system. I need your technical input BEFORE creating Jira tickets. Do NOT write any Terraform code yet — just provide a detailed technical plan.

**Architecture context:**
- Authentication: Amazon Cognito User Pools (ADR-006)
- API Gateway REST + WebSocket with Cognito Authorizer
- Infrastructure: Terraform in `terraform/` root and `terraform/builds/`
- Module structure: `terraform/modules/` has: networking, ecs, rds, lambda, api-gateway, cognito, dynamodb, ecr, s3-cloudfront, secrets
- Environment var files: `terraform/environments/dev.tfvars`

**Current infrastructure state:**
- `terraform/builds/main.tf`: Only S3 builds bucket exists
- No Cognito user pool exists yet
- No API Gateway exists yet
- The `terraform/modules/cognito/` module exists but I need to know what it needs to configure

**Authentication requirements from ADR-006:**
- Cognito User Pool for email/password auth
- JWT tokens validated by API Gateway Cognito Authorizer (frontend calls Cognito SDK directly)
- GlobalSignOut for server-side session invalidation on logout
- Configurable password policy: minimum 8 characters
- Frontend needs: User Pool ID, App Client ID (exposed as env vars / CloudFront config)
- Token expiry: configurable at pool level
- Cognito sub (UUID) mapped to trader_id in PostgreSQL

**Questions I need answered:**

1. What exactly needs to be added to Terraform for Cognito?
2. What API Gateway Cognito Authorizer configuration is needed?
3. How does the frontend get the User Pool ID and App Client ID?
4. What environment variables does the backend (ECS Fargate) need for Cognito?
5. What changes are needed to the GitHub Actions CI/CD pipeline for auth?
6. What is the correct Terraform module structure?

Please provide a concrete technical specification I can use to write Jira tasks with acceptance criteria and implementation details.

## Response Summary

Read all relevant Terraform files across the project to assess current auth infrastructure state: terraform/modules/cognito/main.tf, variables.tf, outputs.tf; terraform/modules/api-gateway/main.tf, variables.tf, outputs.tf; terraform/modules/ecs/main.tf, iam.tf, variables.tf; terraform/modules/secrets/main.tf; terraform/main.tf; terraform/outputs.tf; terraform/variables.tf; terraform/environments/dev.tfvars; .github/workflows/build-frontend.yml and deploy-dev.yml; docs/architecture/adrs/ADR-006-cognito-auth.md. Produced a complete technical specification covering: (1) the Cognito module is already fully implemented and wired into terraform/main.tf; (2) the API Gateway Cognito authorizer is already implemented in the api-gateway module; (3) Terraform outputs cognito_user_pool_id and cognito_web_client_id already exist at the root; (4) the backend does not need Cognito env vars — API Gateway handles JWT validation, and the Cognito sub arrives via the x-amz-cognito-identity-id header; (5) the frontend build pipeline needs a step to resolve Cognito IDs from Terraform outputs and inject VITE_COGNITO_USER_POOL_ID and VITE_COGNITO_CLIENT_ID; (6) the secrets module already has a pts/app-config secret placeholder for cognito_user_pool_id and cognito_client_id that needs a post-Terraform population step in CI/CD.
