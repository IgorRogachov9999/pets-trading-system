# Log Entry - 2026-03-20 17:58:38 UTC

## User Request

add mcp for confluence https://hub.docker.com/r/mcp/atlassian and for zephyr from @smartbear/mcp package

## Response Summary

Added two MCP servers to `ai-env.json` (`mcpServers` array): `atlassian` (Docker transport, image `mcp/atlassian`, with Confluence and Jira env vars mapped from `.env`) and `zephyr` (npx transport, package `@smartbear/mcp`, with `ZEPHYR_API_TOKEN` and `JIRA_BASE_URL`). Created `.mcp.json` with fully resolved values from `.env`. Added `.mcp.json` to `.gitignore` to prevent committing secrets. Noted that `CONFLUENCE_URL` and `CONFLUENCE_USERNAME` in `.env` have swapped semantics (Jira URL vs wiki URL) — corrected the mapping so `CONFLUENCE_URL` in `.mcp.json` points to the wiki URL and `JIRA_URL` points to the Jira URL.
