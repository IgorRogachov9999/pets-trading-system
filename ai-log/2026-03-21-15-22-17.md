# Log Entry - 2026-03-21 15:22:17 UTC

## User Request

You are working on Jira ticket PTS-20: "Infrastructure — Terraform for ECR, S3/CloudFront"

Transition PTS-20 to In Progress when you start, and Done when complete.

## Context

This is part of technical story PTS-17 (Project Base Setup). Create Terraform infrastructure under `terraform/` in the repository at `/Users/ihorrohachov/github/pets-trading-system/`.

This is the **early-stage** infrastructure for storing and deploying the first builds. Full production infra (VPC, RDS, ECS, API Gateway, Cognito) comes in later stories.

## Response Summary

Created the `terraform/` directory from scratch with six `.tf` files and a README. `main.tf` configures the AWS provider (~> 5.0), required Terraform version >= 1.6, and includes a commented-out S3 backend block with a TODO for later promotion to remote state. `variables.tf` defines `aws_region` (eu-west-1), `project_name` (pets-trading-system), and `environment` (dev) with a validation block. `ecr.tf` creates `aws_ecr_repository.trading_api` with scan-on-push and an `aws_ecr_lifecycle_policy` keeping the last 10 tagged images and expiring untagged after 1 day. `s3.tf` creates `aws_s3_bucket.frontend` with versioning, full public access block, and AES256 server-side encryption. `cloudfront.tf` creates an OAC (`aws_cloudfront_origin_access_control`), a bucket policy scoped to the CloudFront service principal, and `aws_cloudfront_distribution.frontend` with redirect-to-https, PriceClass_100, IPv6, and custom error responses for 403/404 → /index.html (SPA routing). `outputs.tf` exports `ecr_repository_url`, `cloudfront_domain_name`, `frontend_s3_bucket_name`, `cloudfront_distribution_id`, and `ecr_repository_arn`. Jira transition calls were attempted but denied by tool permissions — PTS-20 transitions need to be performed manually or permissions granted.
