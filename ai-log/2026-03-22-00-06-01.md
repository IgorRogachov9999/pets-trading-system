# Log Entry - 2026-03-22 00:06:01 UTC

## User Request

Fix the frontend so it calls the correct API Gateway path for the test endpoint.

## Context

API Gateway is configured as follows:
- `GET /health` → no auth → proxies to backend `/api/v1/health`
- `GET /test` → no auth → proxies to backend `/api/v1/test`  (newly added)
- `ANY /{proxy+}` → **COGNITO auth required** → proxies to backend `/{proxy}`

The frontend's `apiFetch` client prepends `VITE_API_URL` (e.g. `https://f1um4y9sn2.execute-api.us-east-1.amazonaws.com/dev`) to whatever path you pass.

## Problem

The frontend is calling `/api/v1/test` — this path hits the catch-all `{proxy+}` route which requires a Cognito JWT, causing a 401 preflight and CORS error.

## Fix

The frontend should call `/test` (the API Gateway path), not `/api/v1/test`. The API Gateway `/test` resource handles the `/api/v1/test` mapping to the backend internally.

1. Read `src/ui/src/api/test.ts` — change the path from `/api/v1/test` to `/test`
2. Read `src/ui/src/features/market/MarketPage.tsx` — verify it uses `fetchTest()`, no changes needed there unless it hardcodes the path
3. Read `src/ui/src/features/market/MarketPage.test.tsx` — update any hardcoded path references if present
4. Run the frontend tests to confirm they still pass: `cd src/ui && npm test -- --run`

Do NOT modify anything else. Do NOT create new files.

## Response Summary

Updated `src/ui/src/api/test.ts` to call `/test` instead of `/api/v1/test` in `fetchTest()`, so the request hits the unauthenticated API Gateway route rather than the Cognito-protected `{proxy+}` catch-all. Confirmed `MarketPage.tsx` delegates entirely to `fetchTest()` with no hardcoded path, and `MarketPage.test.tsx` mocks `globalThis.fetch` at the network level without asserting the URL, so neither file required changes. All 8 frontend tests pass after the fix.
