# Log Entry - 2026-03-21 18:16:18 UTC

## User Request

why does the deploy dev sets automaticaly but for demo is not?

## Response Summary

Explained that the difference between `deploy-dev.yml` and `deploy-demo.yml` CloudFront invalidation steps is a pure inconsistency — no technical reason. Dev uses dynamic AWS CLI discovery of the distribution ID at runtime; demo hardcodes it via a secret. Offered to fix `deploy-demo.yml` to use the same auto-discovery approach, eliminating the `DEMO_CLOUDFRONT_DISTRIBUTION_ID` secret requirement.
