# Log Entry - 2026-03-22 01:04:46 UTC

## User Request

add the same check for demo deploy pipeline, also remove all the triggers except manuall trigger for both of them

## Response Summary

Updated `.github/workflows/deploy-dev.yml`: removed the `workflow_run` trigger block, updated the header comment to say "Manual trigger only", and removed all 4 job-level `if:` conditions (which were only needed for workflow_run gating); the `concurrency: group: deploy-dev` block was retained. Updated `.github/workflows/deploy-demo.yml`: added `concurrency: group: deploy-demo, cancel-in-progress: false` above the `on:` block. Both pipelines are now manual-only (`workflow_dispatch`) with concurrency serialization. No commits made.
