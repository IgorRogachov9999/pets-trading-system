# Log Entry - 2026-03-22 02:12:08 UTC

## User Request

1 - is it safe to use cognito keys on ui instead of my backend to create and get tokens? I mean some cognito data will be publicly visilbe, is it safe? 2 - do not add cahsh and other staff to db for now, only the minimal required data. 3 - use liquibase for migrations, so BE should write migrations scripts in correct format and OPS should update pipelines to run liquibase apply on deployment pipelines before deploying the containers. 4 - customer main page should be called account and url should be /accounts/dashboard, this page should not have logic for now, we implement only auth now. 5 - do not use autogenerated for db ids. 6 - create unit of work for accessing repoistories.

## Response Summary

Answered the Cognito key safety question (yes, safe by design — Pool ID and Client ID are public identifiers equivalent to Firebase apiKey; no secret involved). Updated `docs/spikes/SPIKE-AUTH-000-authentication.md` with 6 changes: (1) minimal traders table schema (id, cognito_sub, email, created_at only — no cash columns); (2) Liquibase migrations replacing raw SQL (BE writes `db.changelog-master.xml` + `changesets/001-create-traders.sql`, OPS adds `liquibase update` step before ECS deploy in `deploy-dev.yml` and `deploy-demo.yml`); (3) post-login redirect changed to `/accounts/dashboard` with `AccountDashboard.tsx` as a stub; (4) DB IDs changed to application-generated `Guid.NewGuid()` with no DEFAULT in the SQL column; (5) IUnitOfWork pattern added (`IUnitOfWork` interface exposes `ITraderRepository Traders` + `CommitAsync`, `UnitOfWork` wraps `NpgsqlConnection` + `NpgsqlTransaction`). Confluence page 1802244 updated to v2 with all changes.
