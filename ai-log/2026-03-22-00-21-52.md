# Log Entry - 2026-03-22 00:21:52 UTC

## User Request

perfect, now all the parts are working fine. Lets update pipelines - 1 Make sure all the deployment and push pipelines are wokring correctly accordint to the latest changes in bootstrap pipeline. 2 Add logic to pipelines to not to push new builds and not to do deployments when there were no changes in repository parts

## Response Summary

Updated three GitHub Actions pipeline files. In `.github/workflows/initial-setup.yml`: added a `detect-changes` job using `dorny/paths-filter@v3` that detects changes in `src/trading-api/**` (backend) and `src/ui/**` (frontend); added `if: needs.detect-changes.outputs.backend == 'true'` to `build-backend` and `if: needs.detect-changes.outputs.frontend == 'true'` to `build-frontend`; added a job-level `if: always() && !cancelled() && needs.terraform-apply.result == 'success' && (needs.build-backend.result == 'success' || needs.build-frontend.result == 'success')` to the `deploy` job; added step-level `if: needs.build-backend.result == 'success'` on ECS, ECS wait, and Lambda deploy steps, and `if: needs.build-frontend.result == 'success'` on S3 and CloudFront invalidation steps. Fixed incorrect frontend S3 bucket names in `.github/workflows/deploy-dev.yml` (changed `${PROJECT_NAME}-dev-frontend` → `${PROJECT_NAME}-frontend-dev`) and `.github/workflows/deploy-demo.yml` (changed `${PROJECT_NAME}-demo-frontend` → `${PROJECT_NAME}-frontend-demo`) to match the actual Terraform bucket naming convention (`${project_name}-frontend-${environment}`), including both the deploy step and the CloudFront discovery step in each file.
