# Log Entry - 2026-03-21 16:05:27 UTC

## User Request

You are working on Jira ticket PTS-25: "TS-005: Terraform — Environment Naming, IAM Audit, and Builds Storage"

Repository root: `/Users/ihorrohachov/github/pets-trading-system/`

## Context

The existing Terraform module structure at `terraform/modules/` has most resource names using `${var.project_name}-${var.environment}` but several modules have hardcoded names or are missing the environment segment. Fix all of them so dev and prod can coexist in the same AWS account.

[full ticket body with 10 fix sections omitted for brevity — see original message]

## Response Summary

Applied all 13 fixes across 10 Terraform files to implement full environment-namespaced resource naming and IAM audit improvements. Key changes: (1) `terraform/modules/dynamodb/main.tf` — table name changed from hardcoded `pts-websocket-connections` to `${var.project_name}-${var.environment}-websocket-connections`; `outputs.tf` — added `table_name` alias output. (2) `terraform/modules/lambda/main.tf` — log group name and function name both updated to include `${var.environment}`. (3) `terraform/modules/ecs/main.tf` — log group name updated to include environment. (4) `terraform/modules/cognito/main.tf` — user pool name and client name both include environment now. (5) `terraform/modules/api-gateway/main.tf` — REST API name, Cognito authorizer name, and all 4 WAF metric name strings updated to include environment. (6) `terraform/modules/ecr/main.tf` — both ECR repo names include environment. (7) `terraform/modules/iam/main.tf` — full rewrite: DynamoDB ARN uses `var.dynamodb_table_name`, ECS log group uses `var.ecs_log_group_name`, Lambda log group uses `var.lambda_log_group_name`, ECR repo ARNs include environment, Lambda function ARN includes environment, `TerraformApply` broad bootstrap policy statement added with comment, `iam:PassRole` for scheduler role added, builds bucket S3 ARNs added to S3 policy; `variables.tf` — added `dynamodb_table_name`, `ecs_log_group_name`, `lambda_log_group_name` variables. (8) `terraform/modules/s3-cloudfront/main.tf` — builds artifact S3 bucket added with versioning, public access block, and SSE; `outputs.tf` — `builds_bucket_name` output added. (9) `terraform/main.tf` — added `ecs_log_group_name` and `lambda_log_group_name` locals; IAM module call updated to pass `dynamodb_table_name`, `ecs_log_group_name`, `lambda_log_group_name`. (10) `terraform/outputs.tf` — `builds_bucket_name` root output added.
