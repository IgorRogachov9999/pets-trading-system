# Log Entry - 2026-03-21 18:13:19 UTC

## User Request

again, what settings do we still need to add to github secrets?

## Response Summary

Reviewed all GitHub Actions workflow files in `.github/workflows/` to identify every `secrets.*` reference. Confirmed 4 secrets are already set (`AWS_REGION`, `PROJECT_NAME`, `TF_STATE_BUCKET`, `TF_STATE_LOCK_TABLE`) and 2 are still pending: `AWS_ROLE_ARN` (output from `initial-setup.yml` Terraform apply for the GitHub OIDC IAM role) and `DEMO_CLOUDFRONT_DISTRIBUTION_ID` (output from `deploy-demo.yml` Terraform apply). `AWS_ROLE_ARN` is the critical blocker as it is required by every workflow that assumes an AWS role.
