# Log Entry - 2026-03-21 12:17:57 UTC

## User Request

use this mcp for microsoft docs { "servers": { "microsoft-learn": { "type": "http", "url": "https://learn.microsoft.com/api/mcp" } } }

## Response Summary

Updated the `microsoft-docs` MCP server in `ai-env.json`, `.mcp.json`, and `ai-env.lock.json` from Docker transport (`mcp/microsoft-learn`, which does not exist) to HTTP transport pointing to `https://learn.microsoft.com/api/mcp` — the official Microsoft Learn remote MCP endpoint. No Docker image required; Claude Code will connect to it directly over HTTP on next restart.
