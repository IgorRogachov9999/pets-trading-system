# Log Entry - 2026-03-21 18:49:56 UTC

## User Request

i think the same should be done for frontend builds storage?

## Response Summary

Investigated whether the frontend builds bucket has the same Terraform state conflict as ECR. Read `terraform/modules/s3-cloudfront/main.tf` and confirmed the builds bucket IS environment-suffixed (`${var.project_name}-${var.environment}-builds`), so there is no state conflict between dev and demo — unlike ECR. Explained the difference and identified a separate operational problem: `initial-setup.yml` only populates `pts-dev-builds`, so `deploy-demo.yml` has nothing to deploy from until `post-merge-build.yml` is run with `environment=demo`. Recommended switching to a single shared builds bucket (`pts-builds`) with its own Terraform state, allowing build-once/deploy-anywhere semantics. Asked user to confirm before implementing.
