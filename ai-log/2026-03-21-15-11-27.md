# Log Entry - 2026-03-21 15:11:27 UTC

## User Request

update the design folder, it should contain 2 folders - docs with description generated by designer, and mockups folder with the design itself generated by the tool. Also update docs folder, add stories folder to store user stories, one folder per story, the folder for story contains md file with story and md files per each tasks for this story. Also add int docs one more tasks folder to store non business tasks md files

## Response Summary

Updated the Project Structure section in `CLAUDE.md`. Expanded `designs/` into two sub-folders: `docs/` (design descriptions and specs written by the designer agent) and `mockups/` (Pencil MCP `.pen` files). Added `docs/stories/` with a pattern of one folder per story (`{story-id}/`) containing `story.md` and per-task `task-{n}.md` files. Added `docs/tasks/` for non-business tasks such as infra, devops, and tech-debt items, each as a standalone `task-{n}.md` file.
