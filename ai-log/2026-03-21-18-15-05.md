# Log Entry - 2026-03-21 18:15:05 UTC

## User Request

why do we need them? why there is no Claudfront for dev env? can you catch them after running the pipeline?

## Response Summary

Read `.github/workflows/deploy-dev.yml`, `deploy-demo.yml`, and `initial-setup.yml` to answer three questions. Explained that `AWS_ROLE_ARN` is needed for GitHub OIDC authentication to AWS. Clarified that `deploy-dev.yml` auto-discovers the CloudFront distribution ID at runtime via AWS CLI query (no secret required), while `deploy-demo.yml` uses a hardcoded secret. Confirmed that `initial-setup.yml` already auto-captures Terraform outputs (including `cloudfront_id`) via job outputs after `terraform apply`, so `DEMO_CLOUDFRONT_DISTRIBUTION_ID` is only needed for subsequent `deploy-demo.yml` runs. Concluded that `AWS_ROLE_ARN` is the only true blocker before running the first pipeline.
