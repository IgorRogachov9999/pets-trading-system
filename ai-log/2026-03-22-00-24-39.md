# Log Entry - 2026-03-22 00:24:39 UTC

## User Request

add one more pipeline terraform-pr-check.yaml, it should work the same as other check pipelines but do check for terraform with plan

## Response Summary

Created `.github/workflows/terraform-pr-check.yml` — a new PR validation pipeline triggered on pull requests to `main` when `terraform/**` files change. The pipeline runs three parallel jobs (`plan-ecr`, `plan-builds`, `plan-dev`) covering all three Terraform workspaces. Each job runs: `terraform fmt -check`, `terraform init` (with S3 backend), `terraform validate`, and `terraform plan` (read-only, no apply). Uses OIDC for AWS authentication matching the same pattern as `initial-setup.yml`. The `plan-dev` job uses `environments/dev.tfvars`.
