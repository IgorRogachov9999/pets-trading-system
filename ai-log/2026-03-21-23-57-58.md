# Log Entry - 2026-03-21 23:57:58 UTC

## User Request

Fix two API Gateway issues in `terraform/modules/api-gateway/main.tf`:

## Problem 1: /test endpoint returns 401

The catch-all `{proxy+}` resource has `COGNITO_USER_POOLS` authorization. The `/test` endpoint on the backend has no auth requirement, but API Gateway blocks it before it reaches the backend.

**Fix**: Add a dedicated `/test` resource with `NONE` authorization — same pattern as the existing `/health` resource. Route it to `http://${var.alb_dns_name}:8080/api/v1/test` via the VPC Link.

## Problem 2: CORS preflight returns 401

OPTIONS preflight requests (sent by browsers before cross-origin fetch) hit the `{proxy+}` route and get rejected with 401 because they carry no JWT.

**Fix**: Add an OPTIONS method with `NONE` authorization and a MOCK integration on the `{proxy+}` resource that returns the required CORS headers:
- `Access-Control-Allow-Origin: '*'` (or the CloudFront domain `https://d2681j5g1s1ydv.cloudfront.net`)
- `Access-Control-Allow-Headers: 'Content-Type,Authorization'`
- `Access-Control-Allow-Methods: 'GET,POST,PUT,DELETE,OPTIONS'`

Use API Gateway MOCK integration type with a 200 response for OPTIONS.

Also add an OPTIONS method on the `/test` resource with the same CORS mock response.

## Important

- Update the `redeployment` trigger hash in `aws_api_gateway_deployment.main` to include the new resources/methods/integrations so changes trigger a redeploy
- Do NOT touch the existing `/health` or `{proxy+}` ANY method resources
- Run `terraform fmt` after making changes
- Do NOT run terraform apply — just update the .tf file

The file is at: `terraform/modules/api-gateway/main.tf`

Read the file first to understand the existing structure before making changes.

## Response Summary

Modified `/Users/ihorrohachov/github/pets-trading-system/terraform/modules/api-gateway/main.tf` to fix two API Gateway issues. Added a dedicated `/test` resource (`aws_api_gateway_resource.test`) with a GET method (`authorization = "NONE"`) and an HTTP_PROXY integration routing to `http://${var.alb_dns_name}:8080/api/v1/test` via VPC Link, mirroring the existing `/health` pattern. Added OPTIONS MOCK integrations with full CORS response headers (`Access-Control-Allow-Origin`, `Access-Control-Allow-Headers`, `Access-Control-Allow-Methods`) on both the `{proxy+}` resource (`proxy_options` method/integration/method_response/integration_response) and the new `/test` resource (`test_options` method/integration/method_response/integration_response), all with `authorization = "NONE"`. The `redeployment` trigger hash in `aws_api_gateway_deployment.main` was expanded to include all 12 new resource/method/integration IDs. `terraform fmt` was run and produced no output (file already well-formatted).
