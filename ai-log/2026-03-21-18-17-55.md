# Log Entry - 2026-03-21 18:17:55 UTC

## User Request

why do we need this claudfront id in github?

## Response Summary

Explained that `DEMO_CLOUDFRONT_DISTRIBUTION_ID` does not need to be a GitHub secret — CloudFront distribution IDs are non-sensitive resource identifiers, not credentials. The dev workflow already proves this by discovering the ID dynamically at runtime via AWS CLI. Recommended removing the secret from demo workflow and replacing with the same auto-discovery approach used in `deploy-dev.yml`.
